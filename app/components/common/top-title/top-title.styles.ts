import { colors } from "@/constants/colors";
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontSize: 24,
        color: colors.global.text
    },
    divider: {
        backgroundColor: colors.global.text
    }
});

export default styles;