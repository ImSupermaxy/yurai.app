import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    content: {
        // width: "100%",
        gap: 18
    }
});

export default styles;